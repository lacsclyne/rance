{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lacsclyne.local/schemas/enemies.schema.json",
  "title": "Enemy collection",
  "type": "object",
  "required": ["version", "enemies"],
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "version": { "type": "integer", "minimum": 1 },
    "enemies": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "name", "faction_id", "rank", "base_stats", "skill_ids"],
        "additionalProperties": false,
        "properties": {
          "id": { "$ref": "#/$defs/contentId" },
          "name": { "type": "string", "minLength": 1 },
          "faction_id": { "$ref": "#/$defs/contentId" },
          "rank": {
            "type": "string",
            "enum": ["minion", "elite", "boss"]
          },
          "base_stats": { "$ref": "#/$defs/stats" },
          "skill_ids": {
            "type": "array",
            "items": { "$ref": "#/$defs/contentId" },
            "uniqueItems": true
          },
          "intent_notes": { "type": "string" }
        }
      }
    }
  },
  "$defs": {
    "contentId": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$"
    },
    "stats": {
      "type": "object",
      "required": ["hp", "attack", "defense", "speed"],
      "additionalProperties": false,
      "properties": {
        "hp": { "type": "integer", "minimum": 1 },
        "attack": { "type": "integer", "minimum": 0 },
        "defense": { "type": "integer", "minimum": 0 },
        "speed": { "type": "integer", "minimum": 0 }
      }
    }
  }
}
