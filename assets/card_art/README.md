# Card Art

Card illustration source files live here.

- Manifest category: `card_art`
- Asset IDs: `card_art.<card_id_suffix>`
- Paths: `assets/card_art/<card_id_suffix>.<ext>`

Placeholder manifest entries may point at future files that do not exist yet
while `required` is `false`.
