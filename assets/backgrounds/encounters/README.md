# Encounter Backgrounds

Encounter and battle-background source files live here.

- Manifest category: `encounter_background`
- Asset IDs: `encounter_background.<encounter_id_suffix>`
- Paths: `assets/backgrounds/encounters/<encounter_id_suffix>.<ext>`

Keep `source_id` aligned with the matching `encounter.<id>` content row.
