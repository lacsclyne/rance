# Portraits

Character portrait source files live here.

- Manifest category: `portrait`
- Asset IDs: `portrait.<character_id_suffix>`
- Paths: `assets/portraits/<character_id_suffix>.<ext>`

Keep `source_id` aligned with the matching `character.<id>` content row.
