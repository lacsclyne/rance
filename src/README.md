# Source Modules

GDScript source should be grouped by module. These directories are placeholders
for later issues; do not add cross-module gameplay logic without first updating
the matching implementation index in `docs/modules/`.
