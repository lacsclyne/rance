# Scenes

Godot scenes live here, grouped by module. The only committed scene for this
framework issue is `scenes/boot/main.tscn`, which exists so the editor can open
and run the project without temporary gameplay logic.
